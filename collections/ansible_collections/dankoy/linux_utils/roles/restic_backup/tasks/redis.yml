- name: Copy restic backup script for redis
  ansible.builtin.template:
    src: restic-backup-redis.sh.j2
    dest: "{{ backup_script_pwd }}/restic-backup.sh"
    owner: root
    group: root
    mode: 0755
  tags: restic-backup

