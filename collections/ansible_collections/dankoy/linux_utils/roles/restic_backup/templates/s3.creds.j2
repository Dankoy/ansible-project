# {{ ansible_managed }}
unset HISTFILE
export RESTIC_REPOSITORY="{{ s3_endpoint }}{{ restic_repo }}"
export AWS_ACCESS_KEY_ID="{{ aws_access_key_id }}"
export AWS_SECRET_ACCESS_KEY="{{ aws_secret_access_key }}"
export RESTIC_PASSWORD="{{ pass.stdout }}"
DB="" 
